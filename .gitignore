# Credentials — exigence du sujet (ne JAMAIS committer)
.env
*.env
!.env.example

# PHP / Composer (pas de dépendances autorisées, mais au cas où)
/vendor/
composer.lock

# Docker
/docker-data/

# Images uploadées par les utilisateurs (étape éditeur)
/public/uploads/

# Logs & temp
*.log
/tmp/

# OS / éditeurs
.DS_Store
.idea/
.vscode/

# Outils locaux (kit agents, etc.)
.freebuff/
