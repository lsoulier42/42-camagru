# Credentials — exigence du sujet (ne JAMAIS committer)
.env
*.env
!.env.example

# PHP / Composer — vendor ignoré, composer.lock versionné (déploiement reproductible)
/vendor/

# PHPUnit
/.phpunit.cache/

# Docker
/docker-data/

# Images uploadées par les utilisateurs (étape éditeur)
/public/uploads/

# Logs & temp
*.log
/tmp/

# OS / éditeurs
.DS_Store
.idea/
.vscode/

# Outils locaux (kit agents, etc.)
.freebuff/
